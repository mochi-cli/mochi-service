{
  "name": "@mochi-cli/service",
  "version": "0.1.0",
  "private": true,
  "description": "Accounts, plans and billing for Mochi Table. Knows who is signed in and what they pay for; never sees a table.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "db:schema": "node --env-file-if-exists=.env.local scripts/apply-schema.ts",
    "check:key": "node --env-file-if-exists=.env.local scripts/check-signing-key.ts",
    "check:env": "node --env-file-if-exists=.env.local scripts/check-env.ts"
  },
  "dependencies": {
    "@google-cloud/kms": "^4.5.0",
    "@neondatabase/serverless": "^0.10.4",
    "@polar-sh/sdk": "^0.49.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
