{
  "name": "@mochi-cli/service",
  "version": "0.1.0",
  "private": true,
  "description": "Accounts, plans and billing for Mochi Table. Knows who is signed in and what they pay for; never sees a table.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "db:schema": "psql \"$DATABASE_URL\" -f src/lib/schema.sql"
  },
  "dependencies": {
    "@google-cloud/kms": "^4.5.0",
    "@neondatabase/serverless": "^0.10.4",
    "@upstash/redis": "^1.34.3",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "stripe": "^17.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
